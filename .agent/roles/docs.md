# Legacy Docs Role Note

Canonical documentation instructions now live in `docs/AGENTS.md`.

Keep this file only as a compatibility pointer for the older custom `.agent/roles/` layout. Update the canonical `AGENTS.md` file instead of editing role rules here.
